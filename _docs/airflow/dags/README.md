<!-- METADATA: {"source_path": "airflow/dags", "source_sha": "5a3ebc7ef626039f4dd50e28e144e98ed16cfc13", "extraction_quality": "full_ast", "model": "gpt-5-mini", "generated_at": "2026-09-08T09:31:18Z", "doc_type": "directory"} -->
[Documentation Home](../../README.md) > [airflow](../README.md) > [dags](./README.md) > **dags**


---


# dags

> **Directory:** `airflow/dags`

## Purpose

This directory contains Airflow DAG definitions for the ecommerce workflow. The contained modules assemble tasks and scheduling information using Airflow primitives to define and orchestrate steps of the ecommerce pipeline.

The code is organized around Airflow constructs and uses BashOperator tasks to run shell commands as pipeline steps, expressing the workflow as one or more DAGs that Airflow can schedule and execute.

## Files

| File | Description |
| --- | --- |
| `ecommerce_pipeline.py` | This module sets up an Airflow-based pipeline for an ecommerce workflow. It imports Airflow's DAG construct and the BashOperator, indicating the file defines at least one DAG and uses BashOperator tasks to run shell commands as steps in that pipeline. The file is organized around Airflow orchestration primitives and is focused on assembling tasks and scheduling them within a DAG context. |

## API

Public classes, interfaces and functions in this directory, plus exported constants, with signatures and line numbers.

### `ecommerce_pipeline.py`

- **`DBT_PROJECT_DIR`** = '/opt/airflow/dbt/ecommerce_dbt' — line 7
- **`DBT_PROFILES_DIR`** = '/home/airflow/.dbt' — line 8
- **`PROJECT_DIR`** = '/opt/airflow' — line 9
- **`extract`** = BashOperator(task_id='extract', bash_command=f'\n python {PROJECT_DIR}/src/ingestion/extr… — line 19
- **`load`** = BashOperator(task_id='load', bash_command=f'\n python {PROJECT_DIR}/src/ingestion/load.py… — line 26
- **`load_to_duckdb`** = BashOperator(task_id='load_to_duckdb', bash_command=f'\n python {PROJECT_DIR}/src/ingesti… — line 33
- **`dbt_build`** = BashOperator(task_id='dbt_build', bash_command=f'\n cd {DBT_PROJECT_DIR}\n dbt build --pr… — line 40

## Key Components

- **`Ecommerce DAG`** (in `ecommerce_pipeline.py`) — The DAG defined in this module is the central orchestration unit that groups tasks, defines their dependencies, and provides the schedule and execution context for the ecommerce workflow within this directory.
- **`BashOperator-based tasks`** (in `ecommerce_pipeline.py`) — Tasks implemented with BashOperator are the actionable steps of the pipeline; they execute shell commands that perform the work of the ecommerce workflow and are the primary runtime elements assembled into the DAG.
- **`Airflow orchestration primitives`** (in `ecommerce_pipeline.py`) — The module's use of Airflow primitives (such as the DAG construct) provides the structure for scheduling, dependency management, and execution semantics that govern how the pipeline runs in an Airflow environment.

## Architecture Notes

This directory is organized as a single-module collection where ecommerce_pipeline.py contains the pipeline definition. That module brings together Airflow constructs and BashOperator tasks to assemble a runnable DAG: it declares the DAG, configures its schedule/context, and defines the individual BashOperator tasks that make up the workflow steps.

When approaching the codebase, start with ecommerce_pipeline.py to see the overall orchestration: the DAG definition shows the schedule and grouping of tasks, and the BashOperator task declarations show the individual steps and their commands. The module is focused on composing these primitives into a scheduled pipeline for the ecommerce workflow.

---

## Navigation

**↑ Parent Directory:** [Go up](../README.md)



---

<sub>This README was generated by [DocBot](https://github.com/marketplace/docbot-by-woden) from the structural analysis of the files in this directory. AI-generated content can contain mistakes — verify against the source code before acting on architectural claims.</sub>
