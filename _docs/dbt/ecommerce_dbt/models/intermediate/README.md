<!-- METADATA: {"source_path": "dbt/ecommerce_dbt/models/intermediate", "source_sha": "25611d310a36cdd034ee8f41959c190fa82747b0", "extraction_quality": "full_ast", "model": "gpt-5-mini", "generated_at": "2026-09-08T09:32:09Z", "doc_type": "directory"} -->
[Documentation Home](../../../../README.md) > [dbt](../../../README.md) > [ecommerce_dbt](../../README.md) > [models](../README.md) > [intermediate](./README.md) > **intermediate**


---


# intermediate

> **Directory:** `dbt/ecommerce_dbt/models/intermediate`

## Purpose

This directory contains intermediate dbt models and their metadata for the ecommerce_dbt project, focused on preparing product-level data for downstream analytics. It includes SQL models that transform staging inputs into curated intermediate tables and a YAML file that documents those models and declares column-level descriptions and tests.

The artifacts here cover both a point-in-time product snapshots dataset and a canonical products dataset, plus the accompanying model metadata and data quality assertions used by dbt to surface schema information and enforce constraints during runs.

## Files

| File | Description |
| --- | --- |
| `int_product_snapshots.sql` | This dbt model defines an incremental materialized SQL select that builds an intermediate product snapshots table. It selects product attributes and pricing fields from a staging source (stg_product_snapshots), computes derived metrics such as discount_amount, final_price, and inventory_value using simple arithmetic and ROUND, and preserves the original snapshot_at timestamp. When run incrementally, the query restricts new rows to those with snapshot_at greater than the … |
| `int_products.sql` | This SQL model builds an intermediate products dataset by reading from a staging products source and projecting a set of product attributes along with calculated pricing and inventory metrics. It first defines a CTE named products that references the staging model stg_products, then selects core identifying and descriptive fields (product_id, product_name, description, category, brand, sku, etc.), pricing and stock fields (price, discount_percentage, rating, stock, weight), … |
| `intermediate.yml` | This YAML file provides metadata for two intermediate dbt models within the ecommerce_dbt project: int_product_snapshots and int_products. Each model block defines a description and a set of columns with human-readable descriptions and associated data tests (such as not_null and unique) that specify expected data quality constraints. The int_product_snapshots model captures point-in-time product information including pricing, discount, computed final price, inventory value … |

## Key Components

- **`Product snapshots incremental model`** (in `int_product_snapshots.sql`) — This model materializes point-in-time product snapshot records and computes derived pricing and inventory metrics (discount_amount, final_price, inventory_value) while preserving snapshot timestamps, making it the primary source in this directory for historical pricing analysis.
- **`Canonical products model`** (in `int_products.sql`) — This model constructs the curated products dataset from staging inputs (via a CTE named products) and projects identifying, descriptive, pricing, and stock attributes that downstream reports and models will rely on for product-level joins and metrics.
- **`Model metadata and tests`** (in `intermediate.yml`) — This YAML document provides the human-readable descriptions and data tests for the two intermediate models, defining expected column semantics and applying not_null/unique constraints that enforce data quality expectations when the models are executed.

## Architecture Notes

The directory is organized around two SQL models that produce intermediate product-focused tables and a single YAML file that documents and tests those models. The SQL files implement the data transformations: one produces point-in-time snapshots with computed metrics and incremental logic, and the other builds a canonical products projection using a products CTE that reads from staging.

The intermediate.yml file complements the SQL models by supplying model descriptions and column-level tests so that dbt can surface schema information and validate data quality. Readers should treat the SQL files as the transformation logic and the YAML file as the source of metadata and test definitions that describe expected structure and constraints for those transformed models.

---

## Navigation

**↑ Parent Directory:** [Go up](../README.md)



---

<sub>This README was generated by [DocBot](https://github.com/marketplace/docbot-by-woden) from the structural analysis of the files in this directory. AI-generated content can contain mistakes — verify against the source code before acting on architectural claims.</sub>
