<!-- METADATA: {"source_path": "dbt/ecommerce_dbt/models/tests", "source_sha": "2d05736edc41475f1d1e33cab8e699919811971b", "extraction_quality": "full_ast", "model": "gpt-5-mini", "generated_at": "2026-09-08T09:34:20Z", "doc_type": "directory"} -->
[Documentation Home](../../../../README.md) > [dbt](../../../README.md) > [ecommerce_dbt](../../README.md) > [models](../README.md) > [tests](./README.md) > **tests**


---


# tests

> **Directory:** `dbt/ecommerce_dbt/models/tests`

## Purpose

This directory contains SQL tests that validate the uniqueness of rows in the analytics model for inventory. The tests focus on identifying duplicate records at the intended grain so downstream reporting and aggregates rely on a consistent unique key.

The contents are targeted, single-purpose SQL checks that run against the compiled dbt models to surface grouping-level duplicates by defined keys and counts.

## Files

| File | Description |
| --- | --- |
| `fct_inventory_unique_grain.sql` | This SQL file defines a query that identifies duplicate records in the fct_inventory model by grouping rows by product_id and snapshot_at and returning groups that have more than one row. It selects product_id and snapshot_at along with a row_count for each group, using a HAVING clause to restrict results to only those groupings where the count exceeds one. The query references the fct_inventory model via dbt's ref function, so it operates on the compiled table or model … |

## Key Components

- **`Duplicate detection query`** (in `fct_inventory_unique_grain.sql`) — This query is the core check in the directory: it identifies groups of rows that violate the expected uniqueness by returning product_id and snapshot_at groupings with a row_count greater than one, making duplicates explicit for investigation.
- **`Grouping keys (product_id, snapshot_at)`** (in `fct_inventory_unique_grain.sql`) — The grouping by product_id and snapshot_at defines the uniqueness grain that this test enforces; these keys determine which combinations are considered duplicates and therefore drive the scope of the test's validation.
- **`dbt ref usage to fct_inventory`** (in `fct_inventory_unique_grain.sql`) — Referencing the fct_inventory model with dbt's ref function ties the check to the compiled model or table used in the runtime environment, ensuring the test evaluates the effective dataset produced by the dbt run.

## Architecture Notes

The tests directory is organized around discrete SQL checks; in this case it contains a single SQL file that performs a targeted validation of the inventory fact model. The SQL groups rows by the specified keys and filters to groupings with more than one row, emitting the grouping keys and a row count for any duplicates found.

Because the test references the fct_inventory model through dbt's ref function, it is intended to be executed in the context of a dbt run or test against the compiled model. For a reader approaching this directory, the single file encapsulates the complete logic for detecting duplicate grains in the fct_inventory dataset and can be inspected or executed to surface offending groupings.

---

## Navigation

**↑ Parent Directory:** [Go up](../README.md)



---

<sub>This README was generated by [DocBot](https://github.com/marketplace/docbot-by-woden) from the structural analysis of the files in this directory. AI-generated content can contain mistakes — verify against the source code before acting on architectural claims.</sub>
