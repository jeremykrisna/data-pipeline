<!-- METADATA: {"source_path": ".github", "source_sha": "c0a0ce156063a2b605ec80ba8db50e86c98b422b", "extraction_quality": "full_ast", "model": "gpt-5-mini", "generated_at": "2026-09-08T09:31:18Z", "doc_type": "directory"} -->
[Documentation Home](../README.md) > [.github](./README.md) > **.github**


---


# .github

> **Directory:** `.github`

## Purpose

Its single configuration file controls how documentation is emitted (per-directory, per-file, or both), where generated docs are stored, which target branches trigger processing, and a set of preferences that shape the content.

The configuration also declares an architecture guide project, per-directory override hooks, a whitelist of file extensions to include in documentation, and a comprehensive set of glob patterns and related settings that define which files are included in or excluded from generated docs. This file is the central place to change documentation behavior for the repository.

## Files

| File | Description |
| --- | --- |
| `wai-docbot.yml` | Specifies how documentation is emitted (per-directory, per-file, or both), where generated docs are stored, which target branches trigger processing, and a set of preferences that shape the content (such as including code links, examples, and whether PR approval is required). The configuration also defines an architecture guide project, per-directory override hooks, a whitelist of file extensions to include in documentation, and a comprehensive set of glob patterns and … |

## Subdirectories

Use these links to drill into the child directory READMEs for this section.

- [workflows](./workflows/README.md)



## Key Components

- **`Documentation emission settings`** (in `wai-docbot.yml`) — These settings determine whether documentation is produced per-directory, per-file, or both and therefore control the overall structure and granularity of generated docs for the repository.
- **`Storage and branch triggers`** (in `wai-docbot.yml`) — Configuration of where generated documentation is stored and which target branches trigger processing defines when and where the output is published, making it essential for CI/CD and release workflows.
- **`Content shaping preferences and inclusion rules`** (in `wai-docbot.yml`) — Preferences such as including code links, examples, PR approval requirements, the architecture guide project, per-directory override hooks, the whitelist of file extensions, and glob patterns determine what content appears in the docs and how specific files or directories are handled.

## Architecture Notes

The configuration file encapsulates the directory's purpose and is the only component to consult when modifying documentation generation settings.

---

## Navigation

**↑ Parent Directory:** [Go up](../README.md)
**🔗 Related:** [workflows](./workflows/README.md)



---

<sub>This README was generated by [DocBot](https://github.com/marketplace/docbot-by-woden) from the structural analysis of the files in this directory. AI-generated content can contain mistakes — verify against the source code before acting on architectural claims.</sub>
