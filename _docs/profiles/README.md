<!-- METADATA: {"source_path": "profiles", "source_sha": "1c368e8dc3434a168466e74c2cfde375f1d5a5d7", "extraction_quality": "full_ast", "model": "gpt-5-mini", "generated_at": "2026-09-08T09:34:20Z", "doc_type": "directory"} -->
[Documentation Home](../README.md) > [profiles](./README.md) > **profiles**


---


# profiles

> **Directory:** `profiles`

## Purpose

This directory holds the dbt profile configuration used to define how dbt connects to local DuckDB databases for the project. It centralizes connection outputs and the active target so that dbt runs can select the appropriate environment (development or production-like) when invoked.

The single file in this directory configures a profile named "ecommerce_dbt" with two outputs (dev and live), each using the DuckDB adapter and pointing at local DuckDB files; it also defines an attached secondary DuckDB file aliased as "warehouse" and sets the active target to "dev" to make the local development settings the default for dbt runs.

## Files

| File | Description |
| --- | --- |
| `profiles.yml` | This YAML file defines a dbt profile named "ecommerce_dbt" with two connection outputs: "dev" and "live". Each output is configured to use the DuckDB adapter and specifies a local DuckDB file path, a thread count, and an attached secondary DuckDB file aliased as "warehouse". The profile sets the active target to "dev", indicating that local development settings are the default connection for dbt runs. |

## Key Components

- **`ecommerce_dbt profile`** (in `profiles.yml`) — This profile is the top-level configuration that groups connection outputs and the active target for the project, serving as the central entry point for dbt to determine which connection settings to use.
- **`dev output configuration`** (in `profiles.yml`) — The dev output contains the local development connection details (DuckDB file path, thread count, and attached warehouse) and is the active target, making it the default environment used when running dbt locally.
- **`live output configuration`** (in `profiles.yml`) — The live output provides an alternative connection configuration intended for production-like runs; it mirrors the structure of dev so the project can switch targets without changing other configuration elements.
- **`warehouse attachment alias`** (in `profiles.yml`) — The attached secondary DuckDB file aliased as "warehouse" is included in each output to expose an additional database file under a consistent alias, enabling models or references that expect a separate warehouse connection to use the same configured alias across environments.

## Architecture Notes

The directory is organized around a single YAML file that consolidates all dbt connection configuration for the project. profiles.yml contains the profile name, multiple outputs for different environments, and the active target selection; together these elements let dbt choose which DuckDB connection (and which attached secondary file alias) to use at runtime.

Because all configuration is kept in this single file, readers should inspect profiles.yml when they need to add or modify connection details, change thread counts, adjust file paths for local DuckDB instances, or switch the active target between dev and live. Each output entry mirrors the same structure (adapter, file path, threads, attached alias) so changes to environment-specific settings are localized within the corresponding output block.

---

## Navigation

**↑ Parent Directory:** [Go up](../README.md)



---

<sub>This README was generated by [DocBot](https://github.com/marketplace/docbot-by-woden) from the structural analysis of the files in this directory. AI-generated content can contain mistakes — verify against the source code before acting on architectural claims.</sub>
